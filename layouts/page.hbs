{{#extend "layout"}}

{{#content "body"}}
  <h1>{{ title }}</h1>
  {{#if subtitle}}
    <p class="subtitle">{{ subtitle }}</p>
  {{/if}}
  <section>
  	{{{ contents }}}
  </section>
{{/content}}

{{/extend}}